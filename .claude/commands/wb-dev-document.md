---
short: Update knowledge store to match current code changes (scan / propose / confirm / apply / report)
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "dev/dev-document-directions", "depth": "full"})`, then start the workflow via `mcp__work-buddy__wb_run("dev-document")`.
