---
short: Commit work-buddy code with test verification, chained doc-update, PII scan, and commit_record
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "dev/dev-commit-directions", "depth": "full"})`, then start the workflow via `mcp__work-buddy__wb_run("dev-commit")`.
