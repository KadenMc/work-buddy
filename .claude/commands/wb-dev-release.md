---
short: Cut a tagged release: preflight gates, consent-gated tag push, CI watch, draft verification, human-only publish.
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "dev/dev-release-directions", "depth": "full"})`, then start the workflow via `mcp__work-buddy__wb_run("dev-release")`.
