---
short: Cleanup, commit, push, and open a PR. Test verification + chained doc-update + PII scan + commit_record.
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "dev/dev-pr-directions", "depth": "full"})`, then start the workflow via `mcp__work-buddy__wb_run("dev-pr")`.
