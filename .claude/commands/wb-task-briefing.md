---
short: Daily task summary
workflow: task_briefing
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "tasks/briefing-directions", "depth": "full"})`, then call `task_briefing`.
