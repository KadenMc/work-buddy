---
description: Investigate whether a task is already done
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "tasks/task-completeness", "depth": "full"})`, then run the workflow with `$ARGUMENTS` as `task_id`.
