---
short: Diagnose component health
workflow: setup_help
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "status/setup-help-directions", "depth": "full"})`, then call `setup_help` with `$ARGUMENTS`.
