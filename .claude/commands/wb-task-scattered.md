---
short: Find scattered tasks
workflow: task_scattered
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "tasks/task-scattered-directions", "depth": "full"})`, then call `task_scattered`.
