---
short: Check Tailscale VPN status
---
Load directions via `mcp__work-buddy__wb_run("agent_docs", {"path": "status/tailscale-status-directions", "depth": "full"})`, then call `mcp__work-buddy__wb_run("tailscale_status")`.
