{
  "email": {
    "kind": "integration",
    "name": "Email integration (Thunderbird bridge)",
    "description": "How work-buddy reads email via the thunderbird-work-buddy companion extension and feeds it into the triage Review pool",
    "content": {
      "full": "## Architecture\n\n```\nwork-buddy agents\n   │  MCP\n   ▼\nwork-buddy capabilities (email_*)\n   │  EmailProvider protocol\n   ▼\nThunderbirdEmailProvider (HTTP client)\n   │  Authorization: Bearer …  on 127.0.0.1:<dynamic_port>\n   ▼\nthunderbird-work-buddy companion extension (separate repo)\n   │\n   ▼\nThunderbird Mail API\n```\n\nThe companion extension is **vendored separately** at `KadenMc/thunderbird-work-buddy`. It does nothing on its own — it exposes a narrow, authenticated, read-only HTTP bridge so work-buddy Python can read mail data. The agent-facing surface lives entirely inside this repo as MCP capabilities.\n\n## Bridge surface (read-only v1)\n\nThe extension exposes a small REST API on a localhost port discovered via a connection file at `<tmpdir>/thunderbird-work-buddy/connection.json`:\n\n| Method | Path                | Use |\n|--------|---------------------|-----|\n| GET    | `/health`           | liveness probe + plugin metadata |\n| GET    | `/accounts`         | enumerate accounts; default-deny allowlist |\n| POST   | `/folders`          | folder tree under an account |\n| POST   | `/messages/recent`  | recent unread/all summaries |\n| POST   | `/messages/search`  | token search across header fields |\n| POST   | `/messages/get`     | fetch one message including body |\n| POST   | `/messages/display` | open a message in Thunderbird UI |\n| POST   | `/messages/exists`  | quarantine probe — \"is this still at this folder URI?\" |\n\nNo compose / send / move / delete / contacts / calendar in v1. Adding any of those requires extension-side trust changes plus capability-side `@requires_consent` gating.\n\n## Provider abstraction\n\n`work_buddy.email.provider.EmailProvider` is a Protocol; backends live under `work_buddy.email.providers.*`. The factory `get_email_provider()` reads `email.provider` from config (default: `\"thunderbird\"`). `\"fake\"` selects an in-memory provider used by tests and dry runs. This abstraction keeps the door open for Gmail API / Microsoft Graph / IMAP backends without touching capabilities or the triage adapter.\n\nKey provider methods:\n- `recent_messages` / `search_messages` — header summaries\n- `get_message` — body fetch\n- `display_message` — open in UI\n- `message_exists` — three-state liveness probe (`True` / `False` / `None` for ambiguous, used by the source-removed quarantine trigger)\n\n## Stable keys vs operational handles\n\n- **Stable key** — derived from RFC 822 Message-ID where available, hash of `(from, date, subject)` otherwise. Survives Thunderbird restarts and folder moves. This is the durable identifier we put on `TriageItem.id` (after hashing) and key dedup off of.\n- **Operational handle** (`EmailMessageHandle`) — the bridge's transient id (Thunderbird's `messageId`) plus the folder URI. Used for follow-up calls (display, get-body, exists). Carried in `TriageItem.metadata` rather than the id.\n\nGmail's labels-as-folders model surfaces the same RFC Message-ID under multiple folder URIs (INBOX, [Gmail]/All Mail, [Gmail]/Important). The triage adapter dedups by stable_key within a single run, preferring inbox > archive > junk > trash via `_FOLDER_TYPE_PRIORITY`.\n\n## Default-deny account access\n\nThe extension's options page presents a checkbox per account; an empty allow-list means **no accounts exposed** (intentionally different from upstream `TKasperczyk/thunderbird-mcp`'s \"empty = all\"). The `email_accounts` capability shows the user which accounts are currently visible.\n\n## Triage integration\n\n`email_triage_run` is the cron-callable entry point: collect recent messages, dedupe, drop into the triage Review pool with `source=\"email_message\"`.\n\n### Verdict-pass modes (Slice 2)\n\nThe LLM verdict pass is gated by `triage.verdict_pass.enabled` (and per-source override `triage.verdict_pass.sources.email.enabled`). Two modes:\n\n- **Off** (default): items land as raw captures (`verdict={\"raw\": true}`) for human review. Slice 1 behavior preserved.\n- **On**: `LLMRunner` per item, structured verdict via `VERDICT_SCHEMA`, mapped into the existing `TRIAGE_ACTIONS` (`close`/`create_task`/`record_into_task`/`leave`/`group`). The agent prompt (`_AGENT_SYSTEM_PROMPT` in `email/capabilities.py`) is biased toward `close` for community events / mailing-list invites / automated notices — its core question is \"would anything materially break if the user never read this?\".\n\n  - Default tier: `FRONTIER_BALANCED`; `local_fast` works for most messages but Qwen's 4096-token window is tight at full body cap.\n  - `include_body_chars` auto-bumps to `_DEFAULT_VERDICT_BODY_CHARS` (800) when verdict pass is on; 0 (headers only) when off. Empirically tuned — bigger and you trip context-overflow on long emails.\n  - When `include_body_chars` overruns context, the LM Studio adapter classifies the error as `context_exceeded` (matched on `n_keep`/`n_ctx` tokens), and `call_for_verdict` auto-escalates to a larger-context tier.\n\n### Per-source verdict-pass gating\n\n`work_buddy.triage.config.is_verdict_pass_enabled_for(cfg, source)` resolves the gate via:\n\n1. `triage.verdict_pass.sources.<source>.enabled` (per-source override; explicit true OR false wins)\n2. `triage.verdict_pass.enabled` (global default)\n3. `False` (default)\n\nKnown source keys (`KNOWN_VERDICT_PASS_SOURCES`): `journal`, `inline`, `email`, `web_browser` (Chrome — reserved for a future capability, key sits there pre-staged).\n\n### Source-removed quarantine\n\nThe `email_message` source descriptor lists `TRIGGER_SOURCE_REMOVED` in its `quarantine_triggers`. The trigger calls `provider.message_exists(handle)` per pending entry on each `triage_pool_sweep` pass. When the bridge says `False`, the entry quarantines with reason `source_removed`. Bridge ambiguity (unreachable, account access changed, malformed handle) returns `None` and **never** quarantines — the unattended sweep must not punish a brief outage.\n\n### Card actions seam\n\nThe `email_message` source descriptor declares an `open_action`:\n\n```python\n\"open_action\": {\n    \"label\": \"Open in Thunderbird\",\n    \"capability\": \"email_display\",\n    \"param_map\": {\n        \"provider_message_id\": \"metadata.provider_message_id\",\n        \"folder_path\":         \"metadata.folder_path\",\n    },\n    \"quarantine_on_error_kinds\": [\"email_message_not_found\"],\n}\n```\n\n`work_buddy.triage.card_actions.build_card_actions` reads this from any source's descriptor and emits an `actions` array on each Review card. The dashboard renders one button per action; click POSTs to `/api/palette/execute`. When the action fails with one of the declared `quarantine_on_error_kinds`, the dashboard auto-fires `triage_pool_quarantine_entry` so the stale card vanishes without waiting for the cron sweep.\n\nThe presentation builder also drops the legacy `url` field for items whose source has `open_action` — handles dead `thunderbird:msg/<key>` synthetic links on pre-Slice-2 pool data.\n\n## Why not extend TRIAGE_ACTIONS\n\nEmail-specific actions like archive / unsubscribe / reply don't currently fit cleanly into `(close, group, create_task, record_into_task, leave)`. Rather than reshape the core schema before the loop is proven, the verdict pass maps email LLM verdicts conservatively into existing actions (newsletter/event invite → `close`, action-required → `create_task`, ambiguous → `leave`). Source-specific verdicts can be added once the dashboard renders email cards distinctly.\n\n## Setup\n\n1. Clone and install the companion extension from `KadenMc/thunderbird-work-buddy`.\n2. In the extension's options, tick the accounts you want exposed.\n3. In `config.local.yaml`: `tools.thunderbird.enabled: true`.\n4. (Optional) Enable verdict pass: `triage.verdict_pass.sources.email.enabled: true`.\n5. Restart Thunderbird and the work-buddy MCP gateway.\n6. `wb_run('email_health')` — should report `ok: true`.\n\nWhen Thunderbird is closed or the bridge is unreachable, the `thunderbird` tool probe fails and the `email_*` capabilities are filtered out of the live registry — `wb_run('feature_status')` shows them under disabled-capabilities with the bridge as the missing dependency.\n\n## Capabilities\n\n- `email_health` — bridge liveness probe.\n- `email_accounts` — list bridge-visible accounts.\n- `email_triage_run` — one BackgroundTriageProducer pass; idempotent. Honors per-source verdict_pass gate; accepts `tier` override; auto-picks `include_body_chars` based on mode.\n- `email_get` — fetch one message body via the operational handle.\n- `email_display` — open a message in Thunderbird's UI (3pane / tab / window).\n\nAll five carry `requires=['thunderbird']` so the registry filters them out when the bridge is unreachable.\n\n## Related\n\n- `triage/card-actions` — the per-source open-in-app action seam used by Review cards.\n- `triage/source-descriptors` — lifecycle TTLs + quarantine trigger registry."
    },
    "tags": [
      "email",
      "thunderbird",
      "triage",
      "bridge",
      "companion-plugin"
    ],
    "aliases": [
      "email integration",
      "thunderbird integration",
      "mail triage",
      "email bridge",
      "work-buddy email"
    ],
    "parents": [],
    "dev_notes": "**Don't reuse the `tools.obsidian.bridge_port` strategy here.** The Thunderbird bridge dynamically picks a port and writes a connection file at `<tmpdir>/thunderbird-work-buddy/connection.json`; the Python client discovers it. A fixed port collides badly when users run multiple Thunderbird profiles. **Don't add mutating routes to the extension without consent gates.** The v1 read-only surface is asserted by `tests/routes.test.cjs` in the extension repo. Any compose / move / delete capability needs `@requires_consent` on the Python side AND an explicit feature flag in the extension's options page. **The `thunderbird` tool probe is intentionally cheap.** It does a TCP probe + one authenticated `/health` round-trip; do not extend it to walk folders or fetch messages. **Verdict pass body-cap interaction with local models.** `_DEFAULT_VERDICT_BODY_CHARS=800` was tuned empirically against Qwen 14B's 4096-token window plus the system prompt + active-tasks context block. Bumping the cap requires either a larger-context model or trimming the prompt. The classifier fix (work_buddy/llm/runner_v2.py + backends/_errors.py 2026-04) means context-exceeded errors auto-escalate now, but you'll burn cycles on a wasted local call before the escalation fires.",
    "entry_points": [
      "work_buddy.email",
      "work_buddy.email.provider",
      "work_buddy.email.providers.thunderbird",
      "work_buddy.email.triage_adapter",
      "work_buddy.email.capabilities"
    ],
    "children": [
      "email/triage-directions"
    ]
  },
  "email/triage-directions": {
    "kind": "directions",
    "name": "Email triage directions",
    "description": "Run one BackgroundTriageProducer pass over recent email and drop new items into the Review pool",
    "content": {
      "full": "## Goal\n\nRun a single email-triage pass — collect recent messages from the configured email provider (Thunderbird bridge by default), dedupe against the existing pool, and drop new captures into the triage Review pool with `source=\"email_message\"`. The user reviews them in the dashboard like any other triage card.\n\n## Steps\n\n1. **Probe.** Call `email_health` first. If it returns `ok: false`, surface the `error_kind` to the user — the most common cause is the bridge being down or no accounts being allowed in the extension's options.\n\n2. **Account-allow check.** If `email_health` is OK but `accessible_accounts == 0`, tell the user to open Thunderbird → Add-ons → Work Buddy Bridge options and tick at least one account. Do not silently produce zero candidates.\n\n3. **Run.** Call `email_triage_run` with the user's preferred window:\n   - default: `days_back=2, max_messages=50, unread_only=true`\n   - if the user asks for \"all recent mail\" relax `unread_only` to false but cap `days_back` to 7\n   - if `dry_run=true` was passed, the pool is not written\n\n4. **Report.** The result dict has `status` (`ok` / `skipped` / `error`), `submitted` count, and a list of un-submitted item IDs with reasons. Prefer numeric summaries over dumping the items dict.\n\n## What you don't do\n\n- **Don't compose / reply / forward / send.** Not exposed in v1. Tell the user to open the message in Thunderbird (`email_display`) and reply there.\n- **Don't extend `TRIAGE_ACTIONS`.** Email-specific verdicts (archive, unsubscribe) are deferred until the dashboard renders email cards distinctly.\n- **Don't bypass the triage substrate.** If the user says \"check my email\" without specifying triage, ask whether they want a manual scan (one-shot via this capability) or recurring triage (the cron job under `sidecar_jobs/`).\n\n## Failure modes\n\n| `error_kind`                  | Meaning                                                   | Action |\n|------------------------------|-----------------------------------------------------------|--------|\n| `email_provider_disabled`    | `email.enabled: false` in config                          | Tell user; suggest config flip |\n| `email_bridge_unreachable`   | Thunderbird closed or extension not running               | User must open TB / install ext |\n| `email_bridge_unauthorized`  | Stale connection-file token (race with TB restart)        | Restart TB to refresh token |\n| `email_message_not_found`    | Follow-up call for a moved/deleted message                | Surface, don't retry |\n| `email_provider_error`       | Generic 4xx/5xx from the bridge                           | Surface verbatim |\n"
    },
    "tags": [
      "email",
      "triage",
      "thunderbird",
      "inbox",
      "directions"
    ],
    "aliases": [
      "triage email",
      "email triage",
      "scan inbox",
      "run email triage",
      "check unread email"
    ],
    "parents": [
      "email"
    ],
    "dev_notes": "**Slice 1 has the verdict pass disabled.** Items land in the pool with `verdict={\"raw\": true}`. If somebody flips `verdict_pass_enabled=True` on the producer, the agent stub raises — that's intentional surfacing of a misconfiguration, not a bug. The proper fix is to wire a real LLM agent like `journal_triage_scan` does, which we've deferred until the dashboard grows email-card rendering.",
    "trigger": "When the user invokes /wb-email-triage or asks to triage / scan / sweep their email inbox",
    "command": "wb-email-triage",
    "capabilities": [
      "email_triage_run",
      "email_health",
      "email_accounts",
      "email_get",
      "email_display"
    ]
  }
}
