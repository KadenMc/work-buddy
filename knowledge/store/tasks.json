{
  "tasks/triage-directions": {
    "kind": "directions",
    "name": "Task Triage Directions",
    "description": "How to run interactive task triage — presentation rules, actions, summary format",
    "aliases": [
      "triage tasks",
      "review inbox tasks",
      "batch task decisions",
      "inbox review"
    ],
    "tags": [
      "tasks",
      "triage",
      "inbox",
      "interactive",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user wants to triage or review pending inbox tasks",
    "command": "wb-task-triage",
    "workflow": "tasks/task-triage",
    "capabilities": [
      "tasks/task_review_inbox",
      "tasks/task_stale_check",
      "tasks/task_change_state"
    ],
    "content": {
      "summary": "Present tasks in batches of 5. Actions: mit, focus, snooze, done, kill, skip. Always ask user — don't decide for them. Report breakdown after applying.",
      "full": "Start the task triage workflow via mcp__work-buddy__wb_run(\"task-triage\"). Follow the conductor.\n\n## Presentation rules\n\nPresent tasks in batches of 5 at a time. For each task show:\n- The task description (trimmed, no raw tags)\n- The suggested action and reason\n- Ask user to confirm, override, or skip\n\nAvailable actions:\n- mit: select as Most Important Task for this week\n- focus: promote to actively working on now\n- snooze: defer (set snooze_until date if possible)\n- done: mark as completed\n- kill: archive/remove (for stale tasks beyond saving)\n- skip: leave as-is for now\n\nState changes are stored in the SQLite metadata store (not inline tags).\n\n## Summary format\n\nAfter applying decisions, report:\n- How many tasks were triaged\n- Breakdown by action taken\n- Current state after triage (focused count, inbox count)\n\n## Don'ts\n\n- Don't present all tasks at once -- batches of 5 maximum\n- Don't make decisions for the user -- always ask\n- Don't recommend system reorganization, new tags, or process changes\n- Don't explain the task management philosophy\n- Don't add overhead -- this should take under 5 minutes\n\nBe efficient. Don't explain the system. Just present tasks and collect decisions."
    }
  },
  "tasks/inline-todos-directions": {
    "kind": "directions",
    "name": "Inline TODOs Directions",
    "description": "How to triage and execute #wb/TODO vault markers — batch presentation, execution rules, tag cleanup",
    "aliases": [
      "process vault todos",
      "inline todos",
      "wb todo markers",
      "handle todos"
    ],
    "tags": [
      "tasks",
      "inline",
      "todos",
      "triage",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user wants to process #wb/TODO markers from across the vault",
    "command": "wb-inline-todos",
    "workflow": "tasks/inline-todos",
    "capabilities": [
      "tasks/task_create"
    ],
    "content": {
      "summary": "Present items in batches of 5. For each: file, full line, parsed instruction (bold), 1-2 lines context. Actions: Handle, Skip, Delete tag. Confirm ambiguous instructions before executing. Never delete lines — only replace #wb/TODO with #wb/DONE.",
      "full": "Start via mcp__work-buddy__wb_run(\"inline-todos\"), then advance with wb_advance.\n\n## Triage rules\n\nPresent items in batches of 5. For each item show:\n1. File: vault path\n2. Line: full line with #wb/TODO highlighted\n3. Instruction: parsed instruction text (bold)\n4. Context: 1-2 lines before/after\n\nActions:\n- Handle -- execute the instruction\n- Skip -- leave for next time\n- Delete tag -- remove tag without executing\n\n## Executing instructions\n\nFor Handle items:\n1. Read the instruction text (freeform natural language)\n2. Use surrounding context to interpret\n3. If clear, execute directly\n4. If ambiguous, ask the user first\n\nReport what was done for each handled item.\n\n## Don'ts\n- Don't execute without showing the user what you'll do\n- Don't modify files outside the cleanup step\n- Don't delete lines -- only replace #wb/TODO with #wb/DONE\n- Don't assume what ambiguous instructions mean -- ask"
    }
  },
  "tasks/weekly-review-directions": {
    "kind": "directions",
    "name": "Weekly Review Directions",
    "description": "How to run the weekly task review — MIT drafting, WIP enforcement, constraint validation",
    "aliases": [
      "weekly review",
      "task review",
      "weekly planning",
      "strategic review"
    ],
    "tags": [
      "tasks",
      "weekly",
      "review",
      "planning",
      "contracts",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user wants to run their weekly task review and planning session",
    "command": "wb-task-review",
    "workflow": "tasks/weekly-review",
    "capabilities": [
      "tasks/task_change_state",
      "tasks/task_briefing",
      "contracts/contract_constraints"
    ],
    "content": {
      "summary": "Agent assembles the picture and proposes; user validates. Under 15 minutes. Every MIT must trace to a contract constraint (max 1 exception). WIP violations require explicit renegotiation.",
      "full": "Start via mcp__work-buddy__wb_run(\"weekly-review\"). Follow the conductor.\n\n## Drafting the plan\n\n1. Review last week's MITs -- which completed? Which didn't? Why?\n2. Check each active contract's constraint -- has it changed? Is it still the real bottleneck?\n3. Propose 3-5 MITs as implementation intentions:\n   Format: \"When [time/trigger], I will [specific action] for [contract]\"\n   Each MIT must trace to a contract constraint. Non-contract tasks get max 1 slot.\n4. Flag issues: WIP violations, no constraint set, snoozed >14 days, inbox needing promotion/kill\n5. Propose state changes: completed -> done, incomplete -> demote/carry, new -> promote\n\n## Validation rules\n\n- Every MIT traces to a contract constraint (max 1 exception for admin/personal)\n- WIP violations require explicit renegotiation -- do NOT silently allow\n- Constraint changes must be specific, not vague\n\nWIP enforcement: If exceeding limit:\n1. State current active contracts\n2. Ask which to pause or complete first\n3. Record the reasoning\n\n## Don'ts\n- Don't present all 50+ tasks -- focus on MITs and flagged items only\n- Don't recommend system changes\n- Don't spend more than 15 minutes\n- Don't create MITs that don't trace to a contract constraint\n- Don't let WIP violations pass without renegotiation\n- Don't make strategic decisions for the user -- propose, don't decide"
    }
  },
  "tasks/handoff-directions": {
    "kind": "directions",
    "name": "Task Handoff Directions",
    "description": "How to write a structured session handoff prompt and package it as a task note",
    "aliases": [
      "handoff task",
      "session handoff",
      "pass off work",
      "create handoff note"
    ],
    "tags": [
      "tasks",
      "handoff",
      "session",
      "continuity",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user wants to hand off in-progress work to a new agent session via a task note",
    "command": "wb-task-handoff",
    "workflow": null,
    "capabilities": [
      "tasks/task_create"
    ],
    "content": {
      "summary": "Write from what you already know -- don't search for additional information. Use the exact section structure: Task, Context, What I Already Know, Key Files, System Notes, Suggested Approach. Create via task_create with summary param.",
      "full": "Create a task with a comprehensive handoff prompt as the task note.\n\n## Write the handoff prompt\n\nConsolidate into these exact sections:\n\n> This is a handoff prompt from session {WORK_BUDDY_SESSION_ID}.\n\n## Task -- What needs to be done. One concrete paragraph.\n## Context -- Why this exists. Motivation without needing to ask.\n## What I Already Know -- Decisions, approaches tried/abandoned, edge cases, constraints. Highest-value section.\n## Key Files -- Absolute paths with brief annotations.\n## System Notes -- Non-obvious environmental knowledge. Skip if nothing applies.\n## Suggested Approach -- If you have a clear idea. Omit if not.\n\nBe specific. File paths absolute. Decisions include rationale.\n\n## Create the task\n\nmcp__work-buddy__wb_run(\"task_create\", {\"task_text\": \"<concise summary>\", \"summary\": \"<full handoff prompt>\"})\n\nThe summary parameter triggers creation of a note file.\n\nIf task_create fails or times out, replay with obsidian_retry using the operation_id from its response:\n\nmcp__work-buddy__wb_run(\"obsidian_retry\", {\"operation_id\": \"op_xxx\"})\n\nThe capability and params are loaded from the operation record — do not re-pass them.\n\n<<wb:obsidian/bridge>>\n\n## Confirm\n\nShow: task ID, brief summary, remind about /wb-task-assign <id>."
    }
  },
  "tasks/assign-directions": {
    "kind": "directions",
    "name": "Task Assign Directions",
    "description": "How to assign a task — presentation format, completion tracking, state change protocol",
    "aliases": [
      "assign task",
      "claim task",
      "pick up task"
    ],
    "tags": [
      "tasks",
      "assign",
      "session",
      "completion",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user runs /wb-task-assign or asks to assign a task to the current session",
    "command": "wb-task-assign",
    "workflow": null,
    "capabilities": [
      "tasks/task_assign",
      "tasks/task_toggle"
    ],
    "content": {
      "summary": "Use MCP tool directly, never Python. task_assign = claim + read; use task_read when you only want to inspect. Present: task text/state/urgency, contract, note preview with path, session count. State NOT changed by assignment. After completing deliverable, proactively ask to mark done — always confirm first.",
      "full": "Assign via mcp__work-buddy__wb_run(\"task_assign\", {\"task_id\": \"<id>\"}). Do NOT use Python code.\n\nIf no argument provided, ask for the task ID.\n\n## Assign vs read\n\n- **task_assign** = read the task AND record the current session against it (claim). Use when the user is starting work.\n- **task_read** = pure read, no session write. Use when you only need to inspect a task (e.g. user is browsing, or an upstream flow wants the payload without implying a claim).\n\nBoth return the same read payload. Internally, task_assign composes task_read with a session-tracker write, so agents can swap call sites without reshaping downstream code.\n\n## Presentation after assignment\n\n1. Task: text and current state/urgency\n2. Contract: which contract it serves (if any)\n3. Note: preview of note content (if exists) + file path\n4. Sessions: how many sessions have worked on this task\n\n## State\n\nTask state is NOT changed by assignment. To mark focused, use task_change_state separately.\n\n## Completion tracking\n\nAfter committing code (or completing the deliverable), proactively ask: \"Task <id> looks complete -- should I mark it done?\"\n\nOn approval: mcp__work-buddy__wb_run(\"task_toggle\", {\"task_id\": \"<id>\", \"done\": true})\n\nDo not silently mark the task done -- always confirm first."
    }
  },
  "tasks/task-read-directions": {
    "kind": "directions",
    "name": "Task Read Directions",
    "description": "How to read a task without claiming it — inspection-only path that does not write a session assignment",
    "aliases": [
      "read task",
      "view task",
      "inspect task",
      "show task",
      "look at task"
    ],
    "tags": [
      "tasks",
      "read",
      "inspect",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user runs /wb-task-read or asks to view a task without claiming it for the current session",
    "command": "wb-task-read",
    "workflow": null,
    "capabilities": [
      "tasks/task_read"
    ],
    "content": {
      "summary": "Pure-read path. Use task_read when the user wants to inspect a task without claiming it for the current session. No session-tracker write, no state change. Use task_assign instead when the user is starting work.",
      "full": "Read via mcp__work-buddy__wb_run(\"task_read\", {\"task_id\": \"<id>\"}). Do NOT use Python code.\n\nIf no argument provided, ask for the task ID.\n\n## When to use task_read vs task_assign\n\n- **task_read** — pure inspection. No session is recorded against the task. Use when the user is browsing, auditing, or you only need the payload for an upstream flow.\n- **task_assign** — inspection PLUS claims the task for the current session. Use when the user is starting work on it.\n\nBoth return the same read payload (task text, metadata, note content, file + line, existing session list). If you assigned a task earlier in the conversation and only want to re-inspect it now, task_read is fine — assigning again just writes an idempotent duplicate session row.\n\n## Presentation\n\nSame as task_assign:\n\n1. Task: text and current state/urgency\n2. Contract: which contract it serves (if any)\n3. Note: preview of note content (if exists) + file path\n4. Sessions: how many sessions have worked on this task (read-only view)\n\n## Do NOT\n\n- Do not propose marking the task done from a read. Completion tracking belongs to the assign flow — a user who only asked to inspect has not committed to finishing it."
    }
  },
  "tasks/briefing-directions": {
    "kind": "directions",
    "name": "Task Briefing Directions",
    "description": "How to present the daily task status summary — concise format, required sections, one next action",
    "aliases": [
      "task briefing",
      "daily task summary",
      "task status"
    ],
    "tags": [
      "tasks",
      "briefing",
      "daily",
      "summary",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user wants a daily task status summary",
    "command": "wb-task-briefing",
    "workflow": null,
    "capabilities": [
      "tasks/task_briefing"
    ],
    "content": {
      "summary": "Run task_briefing and present under 15 lines: focused tasks, overdue, stale alerts, inbox count, archive recommendation. End with ONE recommended next action.",
      "full": "Run mcp__work-buddy__wb_run(\"task_briefing\").\n\n## Presentation format\n\nPresent concisely (under 15 lines):\n1. Focused tasks -- list them (or note \"none focused\")\n2. Overdue -- count and list if any\n3. Stale alerts -- inbox needing triage, forgotten snoozed tasks\n4. Inbox count -- how many waiting\n5. Archive recommendation -- if >10 completed tasks clog the master list\n\nEnd with ONE recommended next action.\n\n## Don'ts\n- Do NOT list every task\n- Do NOT recommend process changes\n- Keep it short -- under 15 lines total"
    }
  },
  "tasks/task-new-directions": {
    "kind": "directions",
    "name": "Task Creation Directions",
    "description": "How to create a task via the task-new workflow — minimal prompting, namespace-tag gate on new namespaces only",
    "aliases": [
      "create task",
      "new task",
      "add task"
    ],
    "tags": [
      "tasks",
      "create",
      "new",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user asks to create a new task",
    "command": "wb-task-new",
    "workflow": "tasks/task-new",
    "capabilities": [
      "tasks/task_create"
    ],
    "content": {
      "summary": "Route user task requests through the task-new workflow. The workflow's DAG enforces plan → enrich → confirm → create; agents don't manually sequence the suggester anymore. Gate only the minting of *new* namespaces, not the application of existing ones.",
      "full": "Route user task requests through the task-new workflow. The workflow's DAG enforces plan → enrich → confirm → create; agents don't manually sequence the suggester anymore. Gate only the minting of *new* namespaces, not the application of existing ones.\n\n## Slice 2 GTD vocabulary (optional)\n\n`task_create` accepts 12 optional kwargs for GTD-shaped metadata: `task_kind` (`task` / `periodic` / `habit`), `density` (`sparse` / `developed`), `outcome_text`, `next_action_text`, `definition_of_done`, `creation_effort`, `user_involvement`, `creation_provenance` (open enum — use `manual` / `agent_inferred_from_journal` / `agent_inferred_from_chrome` / `agent_inferred_from_inline`), `has_deadline`, `deadline_date`, `has_dependency`, `dependency_hint`. Defaults match the legacy assumption (manual, sparse, developed effort, high involvement).\n\nThe task-new workflow's `plan` step doesn't currently emit these fields — they're available for agent-driven creators (Slice 3's Clarify will populate them from captures) and for explicit caller use. Add them to the workflow's plan output if the user asks for a developed task with sub-action-items, or if context provides a clear deadline."
    }
  },
  "tasks/task-scattered-directions": {
    "kind": "directions",
    "name": "Scattered Tasks Directions",
    "description": "How to present scattered task results and triage into action categories",
    "aliases": [
      "find scattered tasks",
      "orphan tasks",
      "tasks outside master list"
    ],
    "tags": [
      "tasks",
      "scattered",
      "triage",
      "directions"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user asks to find tasks scattered across the vault",
    "command": "wb-task-scattered",
    "workflow": null,
    "capabilities": [
      "tasks/task_scattered"
    ],
    "content": {
      "summary": "Present results grouped by file. Assess: stale tasks, active project tasks, duplicates. Suggest concrete actions. Keep output concise.",
      "full": "Run mcp__work-buddy__wb_run(\"task_scattered\").\n\nPresent results grouped by file. For each:\n- File path and task count\n- First few task descriptions (truncated)\n- Any project tags\n\nThen assess:\n1. Stale tasks -- in old journal entries, never migrated\n2. Active project tasks -- in project docs, current but untracked\n3. Duplicates -- appear in multiple locations\n\nSuggest:\n- Which to migrate to master list?\n- Which are stale and should close?\n- Which are project-specific and fine where they are?\n\nKeep concise -- quick scan, not a report."
    }
  },
  "tasks/task-search-directions": {
    "kind": "directions",
    "name": "Task Search Directions",
    "description": "How to search tasks - distinguish description-text search (task_search, store-only) from note-body hybrid retrieval (context_search source=task_note)",
    "content": {
      "full": "There are TWO ways to search tasks. Pick by what you want to find.\n\n## task_search - search by description text (Slice 3, store-only)\n\nLooks at the `description` column of `task_metadata` - the human-readable task line text (`Fix the auth bug`, `Refactor the dashboard`, ...). Bridge-independent: works even when Obsidian isn't running. Cheap, deterministic, no embedding service.\n\n```\nmcp__work-buddy__wb_run(\"task_search\", {\"query\": \"$ARGUMENTS\"})\n```\n\nUse this when:\n  - The user asked \"do I have a task about X?\" and X is a phrase from the task description, not the note body.\n  - You need to look up a task ID from a description.\n  - The bridge is down and you still need to find a task.\n\nReturns task records (full task_metadata rows) ordered most-recently-updated first. Description column was added in Slice 3 - pre-Slice-3 tasks may have NULL description and won't surface here until task_sync runs (<=30 min).\n\n## context_search(source=\"task_note\") - search by note body (hybrid)\n\nHybrid retrieval (BM25 + dense vectors, RRF-fused) over the linked task-note BODIES (the `[[uuid|✍]]`-linked detail files). Doesn't see tasks without notes (the majority of lightweight tasks).\n\n```\nmcp__work-buddy__wb_run(\"context_search\", {\n    \"query\": \"$ARGUMENTS\",\n    \"source\": \"task_note\",\n    \"top_k\": 10\n})\n```\n\nUse this when:\n  - The user is looking for a task by its note content (decisions, context, subtask details).\n  - You want semantic / fuzzy matches, not exact-substring.\n  - The query is natural-language and longer than a few words.\n\n## Method choice on context_search\n\nThe default method (`keyword,semantic`, RRF fused) is right for most natural-language queries.\n\nFor very short queries (<=3 tokens), prefer `method=\"keyword\"`: the dense model adds noise on tiny inputs.\n\nFor exact-string lookups (a UUID, a tag, a code snippet), prefer `method=\"substring\"` - no embedding service required.\n\n## Presentation\n\nFor each hit, surface:\n1. The matching span (description for task_search; chunk text for context_search).\n2. The `task_id` and `task_state` so the user can jump to the task.\n3. Score / rank so multiple competing hits are comparable.\n\nIf zero hits, say so explicitly. If zero hits on `task_search`, consider falling back to `context_search(source=\"task_note\")` - the task may have a note that mentions the query even if the description doesn't.\n\n## Do NOT\n\n- Don't use task_search when the user wants semantic search - it's exact-substring only.\n- Don't use context_search(source=\"task_note\") when the bridge is down - task_search is the bridge-independent path.\n- Don't interpret a hit as a task assignment. Search returns context only. To claim a task, follow up with `task_assign` (or `/wb-task-assign <task_id>`)."
    },
    "tags": [
      "tasks",
      "search",
      "retrieval",
      "hybrid",
      "bm25",
      "dense",
      "directions"
    ],
    "aliases": [
      "search tasks",
      "find task",
      "search task notes",
      "task search",
      "hybrid task search"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user runs /wb-task-search or asks to search task-note bodies for content matching a query",
    "command": "wb-task-search",
    "capabilities": [
      "task_search",
      "context_search"
    ]
  },
  "tasks/task-update-description-directions": {
    "kind": "directions",
    "name": "Task Update Description Directions",
    "description": "How to rewrite a task's description text safely - preferred over filesystem-direct edits, atomic against concurrent user edits",
    "tags": [
      "tasks",
      "update",
      "description",
      "atomic",
      "directions"
    ],
    "aliases": [
      "rewrite task",
      "rename task",
      "edit task description",
      "change task wording",
      "rephrase task"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "agent or user wants to change the description text of an existing task line",
    "command": null,
    "capabilities": [
      "tasks/task_update_description"
    ],
    "content": {
      "summary": "Use task_update_description, NOT filesystem-direct Edit. It routes through the atomic app.vault.process() write path, so concurrent user typing in Obsidian doesn't get clobbered.",
      "full": "## When to use\n\nWhenever a task's description text needs to change - typo fix, scope sharpening, mid-flight rephrase. Examples:\n  - User asks \"can you rename that task to X\"\n  - You're consolidating two scope-shifted tasks and the description needs to reflect the new scope\n  - You spot a typo and want to correct it without deleting + recreating the task (preserves task_id, history, sessions)\n\n## Call\n\n```\nmcp__work-buddy__wb_run(\"task_update_description\", {\n    \"task_id\": \"t-...\",\n    \"new_description\": \"Rewritten task text\"\n})\n```\n\nReturns:\n  - `success`, `task_id`, `old_description`, `new_description`\n  - `old_line` / `new_line` - the full markdown line\n  - `store_updated` - whether the SQLite description column was kept in sync\n  - `atomic` - True when the write went through the atomic `app.vault.process()` path; False / absent when the legacy fallback ran (bridge unreachable)\n  - `conflict_resolved` - True when a concurrent-edit conflict was detected and the retry succeeded\n\n## What it preserves\n\nCheckbox state, the `#todo` marker, all hashtags (`#projects/*`, namespace tags), wikilinks (including the task-note link `[[uuid|📓]]`), the 🆔 marker and ID, plugin emojis (📅 due date, ✅ done date, 🔼/⏫ urgency).\n\n## What it does NOT do\n\n- It will not change the task's state, urgency, due date, or completion. Use `task_change_state` / `task_toggle` for those.\n- It will not add or remove tags. Use `task_set_tags` for tag rewrites.\n- It rejects multi-line descriptions. Task lines are single-line by construction; put detail in the `[[uuid|📓]]`-linked note.\n\n## Why NOT to use filesystem-direct Edit\n\nThe master-task-list.md has a documented read-modify-write race. Pre-Slice-3 the only path for description rewrites was `Edit` on `master-task-list.md`, which can clobber concurrent user typing in Obsidian. The Slice-C atomic write path (`app.vault.process()`) closes that race - but only if you go through `task_update_description`. Filesystem-direct edits bypass it.\n\nIf `task_update_description` returns `success=False, conflict=True`, the user is actively editing the task line in Obsidian. Surface the conflict to the user; don't keep retrying."
    }
  },
  "tasks/task-me-directions": {
    "kind": "directions",
    "name": "Task Me Directions",
    "description": "How to run /wb-task-me — the re-runnable engage flow that answers what should I do right now",
    "aliases": [
      "task me",
      "engage",
      "now plan",
      "what should I do"
    ],
    "tags": [
      "tasks",
      "engage",
      "today",
      "slice-5b"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user runs /wb-task-me or asks what to work on right now",
    "command": "wb-task-me",
    "workflow": "task-me",
    "capabilities": [
      "task_me"
    ],
    "content": {
      "summary": "Re-runnable. Loads context, builds a clamp-to-now plan, presents top 1-2 recommendations. Slice-5a context-aware: skips tasks the agent and user both can't satisfy now.",
      "full": "Run mcp__work-buddy__wb_run(\"task-me\") and follow the conductor.\n\nThe workflow has 4 steps:\n\n1. **load-context** (auto_run) — composes:\n   - task_briefing (focused / mit / overdue / inbox / stale buckets)\n   - calendar (today's events; best-effort)\n   - contract_constraints (WIP limits + active contracts)\n   - the Slice-5a engage view (per-task tier x who_can_act x user_now)\n\n2. **build-now-plan** (auto_run) — generates a clamp-to-now timeline\n   from focused tasks + calendar. Uses the engage view's filtered\n   set so context-blocked tasks don't land in the plan.\n\n3. **engage** (you, reasoning) — present:\n   - Top contract-constraint banner (if any active contracts)\n   - ONE recommendation card with rationale traced to the active\n     contract OR explicitly labeled \"exploration\"\n   - ONE alternative if a sensible second exists\n   - The plan as a compact list (time block + task)\n\n   Keep this brief. V1a — attention scarcity. No more than 2 cards.\n   If everything is blocked on missing contexts, name the contexts\n   and link to /setup.\n\n4. **write-back** (you, reasoning, OPTIONAL) — only if the user\n   explicitly asks to write the plan into the journal Day Planner.\n   Calls day_planner_generate_and_write. Skip silently if not asked.\n\n## Args\n\nThe slash command may be passed an optional preset:\n-  — default; assumes filesystem + vault + web + workstation\n- \n- \n\nThese map to the user_current_contexts list. If unset, the workflow\nruns without filtering — every task surfaces with its blocker badge.\n\n## Re-runnable\n\nDesigned to be called multiple times per day. Each run is independent;\nno state is mutated unless the user opts into write-back.\n\n## Don'ts\n\n- Don't recommend more than 2 actions\n- Don't silently write to the journal\n- Don't treat exploration as paper progress (per CLAUDE.local.md)\n- Don't suggest experiments without an active contract\n\n\n## Slice 5b completion: morning + Today integration\n\nThe morning workflow's `propose-mits` step now chains to task-me via `workflow_ref: task-me` (same pattern as dev-commit's document step -> dev-document).  Run task-me; walk through load-context + build-now-plan + engage; SKIP write-back (the morning's downstream day-planner step writes to the journal).  Advance the morning's propose-mits with `{mits_created: [<task_ids>]}`.\n\nThe Today dashboard tab calls /api/automation/today/write-to-journal (POST, confirm=true) for the consent-gated journal write -- this corresponds to task-me's write-back step when invoked from the dashboard.\n\nToday is now the default landing tab (Slice 5b ROADMAP section 4).  Overview remains accessible in the nav bar."
    }
  },
  "tasks/action-items": {
    "kind": "module",
    "name": "Task action items (Slice 7)",
    "description": "Per-action-item rows attached to a parent task. Each item carries its own risk profile + required contexts + definition_of_done. Safety rule: items with user_authored=0 AND approved_at IS NULL cannot be executed by the agent -- is_executable enforces this.",
    "content": {
      "full": "# tasks/action-items (Slice 7)\n\nModule: work_buddy/obsidian/tasks/action_items.py.\nSchema: task_action_items table + task_metadata.current_action_item_id column.\n\n## Schema\n\nPer-item: id, task_id (FK), sequence (UNIQUE per task), description, state ('pending' | 'in_progress' | 'done' | 'skipped'), risk_profile_json, agent_required_contexts, user_required_contexts, definition_of_done, user_authored, approved_at, completed_at, handoff_package_path, created_at, updated_at.\n\n## CRUD entry points\n\n- create(task_id, *, description, sequence=None, ...) -- auto-assigns next sequence when None.\n- get(item_id), list_for_task(task_id, *, include_done=True), update(item_id, **fields) (sentinel discipline like store.update), delete(item_id).\n- approve(item_id) -- flips user_authored=1 + stamps approved_at (the canonical way for the user to admit an agent-proposed item to execution).\n- set_current(task_id, item_id) -- points task_metadata.current_action_item_id (None to clear).\n\n## Safety rule (ROADMAP section 7)\n\nis_executable(item) returns True iff:\n- user_authored == 1 (user wrote it; no approval needed), OR\n- user_authored == 0 AND approved_at IS NOT NULL (user explicitly approved an agent-proposed item).\n\nAND state is not in {done, skipped} (terminal exclusion).\n\nEvery executor / Resolution Surface accept-action button / develop-at-pickup write step MUST call is_executable before running an item. Helper-level enforcement so future call sites import the check rather than re-implementing.\n\n## position_in_task\n\nposition_in_task(item) -> (1-based-index, total) powers the future master-list \"step N of M\" badge.\n\n## Status\n\nSchema + CRUD ship in Slice 7. The develop-at-pickup LLM flow + the master-list \"step N of M\" frontend + manual /wb-task-develop slash command are deferred -- those depend on Resolution Surface integration.\n\n## Slice 7 completion: markdown <-> table sync\n\n**parse_action_items_from_note(note_body)** parses the `## Action items` section's plain bullets (no checkboxes per Slice 7 doctrine).  Empty section is meaningful -- represents explicitly-sparse intent; the sync layer does not auto-fill.\n\n**reconcile_from_markdown(task_id, note_body)** is the per-task diff-and-apply.  Pairs markdown bullets to table rows by sequence index.  Adds new bullets, updates changed descriptions in place (preserving state + risk_profile + contexts + approval), deletes table rows that no longer have a corresponding bullet.  Idempotent on re-run.\n\nAdoption-flips-authorship: if an agent-proposed item appears in the markdown (the user kept it after develop-at-pickup), user_authored is flipped to 1 idempotently -- markdown edits ARE user authorship by definition.\n\n**migrate_existing_notes(read_note_body)** is the one-shot migration for legacy developed-task notes.  Walks every non-sparse task with a note_uuid; calls reconcile_from_markdown.  Idempotent.  Skips missing notes (transient bridge failure shouldn't clobber the table).\n\n## task_sync hook\n\nPer task_sync run: walks every surviving developed/dense task with a note_uuid and calls reconcile_from_markdown.  Sparse tasks are SKIPPED (the explicit-sparsity doctrine).  Bridge unavailability is non-fatal; the next sync pass catches up.  Summary lands on the task_sync result as `action_items: {tasks_examined, added, updated, deleted, kept}`."
    },
    "tags": [
      "tasks",
      "action-items",
      "density",
      "slice-7",
      "develop-at-pickup",
      "safety-rule"
    ],
    "aliases": [
      "action items",
      "task action items",
      "task_action_items",
      "is_executable",
      "approve action item",
      "current step",
      "step N of M"
    ],
    "parents": [
      "tasks"
    ],
    "entry_points": [
      "work_buddy.obsidian.tasks.action_items.create",
      "work_buddy.obsidian.tasks.action_items.list_for_task",
      "work_buddy.obsidian.tasks.action_items.update",
      "work_buddy.obsidian.tasks.action_items.approve",
      "work_buddy.obsidian.tasks.action_items.set_current",
      "work_buddy.obsidian.tasks.action_items.is_executable",
      "work_buddy.obsidian.tasks.action_items.position_in_task",
      "work_buddy.obsidian.tasks.action_items.parse_action_items_from_note",
      "work_buddy.obsidian.tasks.action_items.reconcile_from_markdown",
      "work_buddy.obsidian.tasks.action_items.migrate_existing_notes"
    ],
    "dev_notes": "Markdown is the eventual canonical surface (ROADMAP section 8 footnote). Slice 7 ships the table; the markdown round-trip lands in a follow-up so the schema can stabilize against real usage first. set_current uses raw SQL because store.update does not yet take the Slice-7 column -- when other callers need to write current_action_item_id, add the kwarg to store.update like the Slice-5a fields."
  },
  "tasks/working-on-now": {
    "kind": "module",
    "name": "Working-on-now (Slice 5b session focus)",
    "description": "Per-session 'what is THIS session actively working on' pointer. Allen-style one-current rule, scoped per agent session.  The Today tab honors this above pure heuristics when ranking recommendations: a stated focus bubbles to position 1.",
    "content": {
      "full": "# tasks/working-on-now (Slice 5b)\n\nModule: work_buddy/obsidian/tasks/session_focus.py.\nSchema: session_focus table (session_id PK, task_id FK, started_at).\n\n## Why per-session\n\nThe `focused` task state (Slice 2) is persistent and Allen-style sticky -- a task stays focused until the user explicitly moves it.  `working_on_now` is the orthogonal axis: 'I'm actively head-down on this RIGHT NOW.'  Two parallel agent sessions can each have their own working_on_now without contention, and clearing it is cheap.\n\n## API\n\n- set_working_on_now(session_id, task_id) -- idempotent, INSERT OR REPLACE so the per-session uniqueness invariant holds without a separate DELETE round-trip.\n- get_working_on_now(session_id) -> {task_id, started_at} | None.\n- clear_working_on_now(session_id) -> bool.\n- sessions_focused_on(task_id) -> list -- reverse lookup for the dashboard's 'X sessions actively working on this' pill.\n- all_active() -> list -- used by the Today recommender to promote stated focus.\n\n## HTTP\n\n- POST /api/tasks/<task_id>/working-on-now -- set the pointer.  body: {session_id} (or X-Work-Buddy-Session header).\n- DELETE /api/tasks/<task_id>/working-on-now -- defensive clear (only if existing pointer matches the given task_id).\n\n## Today-tab integration\n\nThe Today payload's `recommendations` list is computed by `_ranked_recommendations_with_session_focus` -- it pulls all active session_focus rows and bubbles their target tasks above the Slice-5b deterministic heuristic.  The full focus set is also surfaced as `session_focus` on the payload so the frontend can render 'you said you're working on X'.\n\n## Why a separate table, not a column on task_metadata\n\n(a) Multiple sessions can each have their own working_on_now; a single column on task_metadata would force one global answer.  (b) Clearing is cheap (DELETE one row) versus nullable-column update churn.  (c) The pattern matches task_sessions (the existing assignment-tracking table) -- another per-session relation, not a per-task scalar."
    },
    "tags": [
      "tasks",
      "session-focus",
      "working-on-now",
      "slice-5b"
    ],
    "aliases": [
      "working on now",
      "session focus",
      "current focus",
      "what am I working on"
    ],
    "parents": [
      "tasks"
    ],
    "entry_points": [
      "work_buddy.obsidian.tasks.session_focus.set_working_on_now",
      "work_buddy.obsidian.tasks.session_focus.get_working_on_now",
      "work_buddy.obsidian.tasks.session_focus.clear_working_on_now",
      "work_buddy.obsidian.tasks.session_focus.sessions_focused_on",
      "work_buddy.obsidian.tasks.session_focus.all_active"
    ],
    "dev_notes": "No automatic cleanup -- session_focus rows persist until explicitly cleared OR the parent task is deleted (CASCADE).  This is intentional: the cleanup discipline is the caller's (slash command on session end, or the user clicks 'stop working on this').  Adding TTL-based expiry would discard valid focus from long-running sessions; the cost of a stale row is minor (recommender just fails to find the task)."
  },
  "tasks/task-develop-directions": {
    "kind": "directions",
    "name": "Task Develop Directions",
    "description": "How to run /wb-task-develop -- LLM-driven decomposition into ordered action items with per-item user review and the ROADMAP section 7 hallucination gate.",
    "aliases": [
      "develop task",
      "decompose task",
      "break task into action items",
      "develop at pickup",
      "task develop"
    ],
    "tags": [
      "tasks",
      "develop",
      "decompose",
      "slice-7"
    ],
    "parents": [
      "tasks"
    ],
    "trigger": "user runs /wb-task-develop OR engage flow auto-invokes when compute_pickup_readiness returns ready=False",
    "command": "wb-task-develop",
    "workflow": "task-develop",
    "capabilities": [
      "develop_propose",
      "develop_apply"
    ],
    "content": {
      "summary": "Run the task-develop workflow via mcp__work-buddy__wb_run('task-develop', {'task_id': '<id>'}).  LLM proposes ordered action items; you review each one with the user; only accepted items get persisted with approved_at set.",
      "full": "Run mcp__work-buddy__wb_run('task-develop', {'task_id': '<id>'}) and follow the conductor.\n\n## Three steps\n\n1. **propose** (auto_run via develop_propose) -- LLM decomposition.  Returns proposed items + rationale + confidence.  Empty items[] = LLM judged the task atomic.\n\n2. **review** (you, reasoning) -- present each item to the user.  Edit-each-item affordance: the user can accept, reject, or edit the description / definition_of_done / contexts before accepting.  REJECTED items are DISCARDED -- do not silently keep them as 'pending review' (that breaks the hallucination gate).\n\n3. **apply** (you, reasoning) -- call develop_apply with the accepted subset.  Items land with user_authored=0 + approved_at=<now>.  set_current_to_first=true points the master-list 'step N of M' badge at the first item.\n\n## Why per-item approval\n\nROADMAP section 7 hallucination gate: items with user_authored=0 AND approved_at=NULL CANNOT be executed by the agent under any tier -- not even tier 4.  This protects against agent-invented sub-actions silently running.  The approval flow (per-item, explicit) is what admits items to executability.  Skipping the user review step is NEVER acceptable -- if the user says 'just do it,' you advance the review step with all items accepted; do not bypass develop_apply altogether.\n\n## Quality bar\n\n- Each item is a single concrete physical step.\n- Order matters; sequence reflects execution order.\n- Empty list IS valid (the task is correctly atomic).\n\n## Don'ts\n\n- Don't loop back to propose during review -- one-shot with edit-each-item per Q-7.\n- Don't silently mark unapproved items as approved.\n- Don't run develop_apply with an empty approved_items list -- the function refuses for safety; if the LLM concluded items=[], advance the workflow with that decision but do not call apply."
    }
  }
}