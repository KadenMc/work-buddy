export interface HelpContent {
  /** One concise, user-facing TL;DR sentence. */
  readonly summary: string;
  /** One short paragraph explaining mechanism, scope, or important exceptions. */
  readonly details: string;
}
